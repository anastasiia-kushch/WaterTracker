<img width="666" height="1294" alt="Снимок экрана 2025-12-01 в 22 58 29" src="https://github.com/user-attachments/assets/d579d53f-275f-48fc-93da-a817ba5f6d2d" />

## Зміни та логіка рішень

- Тема інтерфейсу: додано повноцінну підтримку світлої/темної тем у всіх екранах і компонентах.

  - Стан: Redux `themeSlice` (`toggleTheme`, `setTheme`).
  - Кольори: централізовано через `styles/colorsTheme.ts` та функцію `getColors(theme)` у `styles/colors.ts`.
  - Динамічні стилі: створюються через `useMemo` на основі `theme`, що виключає флікер та зайві перерендери.

- Оновлені екрани: `HomeScreen`, `SettingsScreen`, `HistoryScreen`, `DayDetailsScreen`, `LoginScreen`, `SignupScreen`.

  - Кожен екран отримує `theme` з Redux (`useSelector`) і будує стилі через `getColors(theme)`.
  - Для календаря додано узгоджені кольори теми (виділення, фон, текст).

- Оновлені компоненти: `Header`, `BasicButton`, `ProgressCircle`, `Input`, `AddCustomAmountModal`, `LogoutModal`.

  - Кольори і стилі тепер залежні від теми через `getColors(theme)`.
  - Кнопки мають коректні стани натиску для обох тем.

- UX/технічні виправлення:

  - Уникнення інлайн-стилів у критичних місцях та створення стилів через `StyleSheet.create` для продуктивності.

- Як перемикати тему: у `Settings` натисніть кнопку “🌞 Light mode / 🌚 Dark mode” — стан збережеться в Redux і миттєво оновить UI.

Ці зміни уніфікували управління кольорами, спростили підтримку тем і прибрали джерела можливих помилок при розширенні інтерфейсу.
